import React from "react";
import SongsList from "../../redux/containers/SongsList";

const MainPage = () => {
  return (
    <React.Fragment>
      <SongsList />
    </React.Fragment>
  );
};

export default MainPage;
